"""
datas 안에서 collections 모든 인덱스를 배열로 리턴 없으면 x를 찍고 다 없으면, 빈 배열
"""


def solution(datas, collections):
    n = len(datas)
    arr = []
    for i in range(0, n):

    return arr


datas = [15, 16, 17, 25, 61]
collections = [25, 61, 666]
print(solution(datas, collections))
